let arr = [10, 20, 30, 40]
// console.log(arr);

// Push : Add elements at the end of array
arr.push(50)
// console.log(arr);

// Remove element from the end of array
arr.pop()
// console.log(arr);

// Remove element from the starting
arr.shift()
// console.log(arr);

// Add elements in the starting of array
arr.unshift(69)
// console.log(arr);

// Find : Return the first element that satisfies the provided testing function
let array = [1,2,3,4]
let found = array.find((element) => element > 2)
// console.log(found);

// Includes : Return true or false
// console.log(arr.includes(30)); // True
// console.log(arr.includes(50)); // False

// Concat : Used to merge the array
let arr1 = [10, 20]
let arr2 = [30, 40]
let merged = arr1.concat(arr2)
// console.log(merged);

// Join : Array ko string mai convert kar sakte hai with a seperator also.
let arr3 = ["Jatin", "SDE", "Amazon", "Microsoft"]
let result = arr3.join(" : ")
// console.log(result);

// Splice : Add ya remove karta hai element of array mai se
let arr4 = [1,2,3,4]
let element = arr4.splice(2,1,'a')
// console.log(arr4);

// Slice










