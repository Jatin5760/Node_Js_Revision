// console.log("I'm Learning advance Loops");

const phone = {
    brand:'iphone',
    model:16,
    price: 75000,
    ram: "8gb",
    rom: "128gb",
};

// For in loop : Generally print the key value of the loop
for(let key in phone){
    console.log(key);
}
